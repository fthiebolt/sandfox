import { JStringifyPipe } from './jstringify.pipe';

describe('JStringifyPipe', () => {
  it('create an instance', () => {
    const pipe = new JStringifyPipe();
    expect(pipe).toBeTruthy();
  });
});
