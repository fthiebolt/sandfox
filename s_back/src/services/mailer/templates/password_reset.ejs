<!DOCTYPE html>
<html lang="fr">

<head>
	<title>Reinstallation de mot de passe</title>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<style>
		:root {
			background-color: rgb(235, 235, 235);
			--background-light: #194052;
			--background-dark: #091A23;
			--background-primary: rgb(235, 235, 235);
		}

		.container {
			height: fit-content;
			width: 600px;
			color: rgb(20, 20, 20);
			background: var(--background-primary);
			font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
			margin: auto;
		}

		.btn {
			text-decoration: none;
			color: rgb(235, 235, 235);
			padding: .75rem;
			border-radius: .5rem;
			background-color: var(--background-light);
			margin: auto;
			transition: all .5s ease-out;
		}

		.btn:hover,
		.btn:focus {
			background-color: var(--background-dark);
		}

		@media (max-device-width: 600px) {
			:root {
				font-size: 8px;
			}

			.container {
				width: 300px;
			}

		}
	</style>
</head>

<body>

	<div class="container">
		<a href="<%=process.env.FRONT_HOST%>"><img src="<%=process.env.FRONT_HOST%>/assets/icons/logo_sandfox.png"
				style='width:15rem;height:auto;display: block;margin-right:auto;padding: 1rem;'></a>
		<h1>Reinstallation de mot de passe</h1>
		<h3>Bonjour
			<%= user.first_name %>,</h3>
		<p>
			Afin de reinstaller votre mot de passe, cliquez sur le lien ci dessous:
			<br>
			<br>
			<a href="<%=process.env.FRONT_HOST%>/reset?user_id=<%= token.user_id %>&token=<%= token.token %>"
				class="btn">Reinstaller le mot de passe</a>
			<br>
			<span class="gray">
				En cas de problème, vouillez contacter le responsable.
			</span>
			<br>
			Cordialement,
		</p>
	</div>
</body>

</html>
